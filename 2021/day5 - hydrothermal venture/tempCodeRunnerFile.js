const dx = x1 > x2 ? -1 : 1;
      // const dy = y1 > y2 ? -1 : 1;
      // let y = y1;
      // for (let i = x1; x1 > x2 ? i >= x2 : i <= x2; i += dx) {
      //   const k = key(i, y);
      //   y += dy;
      //   visited[k] = (visited[k] || 0) + 1;
      //   if (visited[k] === 2) count++;
      // }